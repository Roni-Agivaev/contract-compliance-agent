# Copy this file to `.env` and fill in real values. `.env` is gitignored.

# LLMod.ai (shared group key). Paste your NEW key on the line below.
LLMOD_API_KEY=

# Pinecone (vector DB for the legal corpus)
PINECONE_API_KEY=

# Supabase (primary DB: minimum_wage table)
SUPABASE_URL=
SUPABASE_KEY=
